name: GH200 Failure Control

on:
  workflow_dispatch:

jobs:

  test:
    runs-on: ubuntu-latest

    steps:
      - name: Expected failure
        continue-on-error: true
        run: |
          echo "This step will fail"
          exit 1

      - name: Next step
        run: echo "I still run"
