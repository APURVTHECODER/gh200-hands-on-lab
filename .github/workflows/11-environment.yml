name: GH200 Environment Approval

on:
  workflow_dispatch:

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Build application
        run: |
          echo "Building application..."
          echo "Build complete"

  deploy:
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: production

    steps:
      - name: Deploy
        run: |
          echo "Deploying to PRODUCTION"
          echo "Deployment approved!"
